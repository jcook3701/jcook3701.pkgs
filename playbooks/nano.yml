# SPDX-FileCopyrightText: Copyright © 2026, Jared Cook
# SPDX-License-Identifier: AGPL-3.0-or-later
---
- name: Install nano to localhost
  hosts: localhost
  gather_facts: true
  become: true
  tasks:
    - name: Install nano directories
      ansible.builtin.include_role:
        name: pkg-install
        tasks_from: pkgs-common/nano/nano-install.yml
