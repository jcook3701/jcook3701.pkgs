# systemd-resolved

systemd-resolved is a system service in Linux that provides network name resolution (DNS, mDNS, and LLMNR) to local applications. It acts as a local stub resolver, handling DNS caching, encryption via DNS-over-TLS (DoT), and routing specific domain queries to specific network interfaces (Split DNS)
