# SPDX-FileCopyrightText: Copyright © 2026, Jared Cook
# SPDX-License-Identifier: AGPL-3.0-or-later
---
tftpd_hpa_directory:
  conf: "{{ tftpd_hpa_os_directory.conf }}"
  root: "{{ tftpd_hpa_os_directory.root }}"

tftpd_hpa_file:
  conf: "{{ tftpd_hpa_os_file.conf }}"
