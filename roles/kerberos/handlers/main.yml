# SPDX-FileCopyrightText: Copyright © 2026, Jared Cook
# SPDX-License-Identifier: AGPL-3.0-or-later
---
- name: Restart Kerberos Server Services
  ansible.builtin.systemd:
    name: "{{ service }}"
    state: restarted
  loop: "{{ kerberos_services }}"
  loop_control:
    loop_var: service
