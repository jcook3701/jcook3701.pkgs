# LightDM

LightDM is a lightweight, cross-desktop display manager (login screen) for Linux that manages X and Wayland servers, user logins, and graphical greeters. Known for being fast and highly customizable, it is used by default in various Linux distributions like Xubuntu and Mint. It supports multiple desktop environments and provides a simple, efficient interface for starting user sessions.
